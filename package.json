{
  "name": "mini-projects",
  "version": "0.0.1",
  "author": "TatsuyaYamamoto",
  "scripts": {
    "deploy": "    npm run build && firebase deploy --project minis-development",
    "deploy-pro": "npm run build && firebase deploy --project minis-production",
    "build": "run-p build:*",
    "build:pop-team-crash": "npm run --prefix projects/pop-team-crash build"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.2"
  }
}
